# Dice UI (Stake Originals) — Mocked Frontend

Локальная UI-реализация игры **Dice (Stake Originals)** с максимально простым mock-сервером.  
Проект предназначен **исключительно для разработки и тестирования пользовательского интерфейса**.

Бизнес-логика, математика и provably fair **осознанно упрощены**.

---

## 🎯 Цель проекта

- Воссоздать **UI / UX Dice от Stake**
- Работать **только через API**
- Использовать **тупой, предсказуемый mock server**
- Быть удобным для:
  - UI-разработки
  - автотестов
  - AI-агентов (Claude Code)

---

## 🧠 Важный принцип

> **UI не знает, как работает Dice.  
> UI знает только API.**

Любая логика:
- выигрыша
- расчёта выплат
- изменения баланса  

👉 находится **строго в mock-сервере**.

---

## 🧱 Технологический стек

- **Vite**
- **React + TypeScript**
- **Tailwind CSS**
- **Mock Server** (stateful, `Math.random`)
- **Claude Code** (agent-driven development)

---

## 📁 Структура репозитория

```text
dice-ui-mock/
│
├─ README.md
│
├─ /agents/
│   └─ ui.agent.md            # контракт UI-агента
│
├─ /knowledge/
│   └─ dice-ui.behavior.md    # UI-спецификация
│
├─ /mock-server/
│   ├─ handlers.ts            # API endpoints
│   ├─ state.ts               # in-memory state
│   └─ api.md                 # API контракт
│
├─ /src/
│   ├─ ui/
│   │   ├─ components/
│   │   ├─ hooks/
│   │   └─ pages/
│   ├─ api/
│   │   └─ dice.ts            # API client
│   └─ main.tsx
│
└─ vite.config.ts
